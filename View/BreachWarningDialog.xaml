<Window x:Class="PasswordManager.View.BreachWarningDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Security Warning" 
        Height="250" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="⚠" 
                       FontSize="24" 
                       Foreground="#de0d0d"
                       Margin="0,0,10,0"
                       VerticalAlignment="Center"/>
            <TextBlock Text="Password Breach Warning" 
                       FontSize="16" 
                       FontWeight="Bold"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="MessageText" 
                       TextWrapping="Wrap" 
                       FontSize="13"
                       LineHeight="20"
                       Foreground="#333"/>
        </ScrollViewer>

        <Button Grid.Row="2" 
                Content="OK" 
                Click="OkButton_Click"
                Padding="30,8"
                Margin="0,15,0,0"
                HorizontalAlignment="Center"
                Background="#de0d0d"
                Foreground="White"
                BorderThickness="0"
                FontSize="13"
                Cursor="Hand">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                        Padding="{TemplateBinding Padding}"
                                        CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#de0d0d"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

    </Grid>
</Window>