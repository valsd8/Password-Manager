<UserControl x:Class="PasswordManager.View.EnterPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordManager.View"
             xmlns:vm="clr-namespace:PasswordManager.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    
    <UserControl.Resources>
        
        
    </UserControl.Resources>

    <Grid Background="#F0F0F0">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <TextBlock 
            Text="Successfully loaded file. Please enter your password to decrypt it." 
            Style="{StaticResource ModernTextBlockStyle}"
            Margin="0,0,0,10"/>

            <PasswordBox 
            x:Name="PasswordInput" 
            Style="{StaticResource ModernPasswordBoxStyle}" 
            Margin="0,0,0,10"/>

            <Button 
            Name="PasswordSubmitButton" 
            Content="Submit Password" 
            Command="{Binding SubmitPasswordCommand}" 
            CommandParameter="{Binding ElementName=PasswordInput}"
            Style="{StaticResource ModernButtonStyle}"
/>

            <TextBlock Name="UpdateAboutPasswordDecryption" Text="{Binding MyText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </Grid>

</UserControl>
